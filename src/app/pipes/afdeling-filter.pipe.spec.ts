import { AfdelingFilterPipe } from './afdeling-filter.pipe';

describe('AfdelingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AfdelingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
